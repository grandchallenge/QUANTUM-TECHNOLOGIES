# QTR-C90-RETAINED-BOUND-001

Status: `AUTHORIZED_EXECUTION__PREOUTCOME_PACKAGE_FROZEN`

## Authority

- Council: #105.
- Referee: #105 comment `5390019257`.
- Human Steward: #105 comment `5391086069` — `ADOPT_WITH_AMENDMENTS__AUTHORIZE_QTR_C90_RETAINED_BOUND_001_NON_MATERIALIZING_EXECUTION_ONLY`.
- Execution docket: #106.
- Protected predecessor/main: `b7e5127fed532a8bc6dc6703bfcac3f58882477f`.
- Protected resource-envelope evidence payload: `0ea802c6ce9c584c52bbc5608ac4a94abec5f29c2939e5c226386c4581205195`.
- Manifest payload: `d9072de9631901a0b97df61c14b8c5dc9d5de7d21ad9d7e181765c69fad223c2`.

## Question

Can the number of reachable **unique canonical structural nodes** for the same frozen C90 symbolic compiler be bounded substantially below the protected intern-attempt upper bound without constructing the C90 DAG?

This is a cardinality-certificate programme. It does not change the compiler, method family, elimination orders, cumulative-work coordinates, physical model, or resource caps.

## Frozen hierarchy

`B0_ATTEMPT_UPPER_CONTROL`: protected node-intern-attempt upper bound.

`B1_TYPED_CANONICAL_KEY_BOUND`: exact terminal and I-node key domains plus typed binary-key buckets, with every binary bucket capped by its exact intern-attempt count.

`B2_STAGE_OPERAND_DOMAIN_BOUND`: inductive count-only propagation of factor support cardinalities through frozen elimination stages. For operand pools of certified sizes `L,R`, a binary-key bucket is bounded by `min(attempts, L*R)`; marginal pairing from one common pool of size `N` is bounded by `min(attempts, N(N+1)/2)`.

`B3_PROVENANCE_PARTITION_BOUND`: B2 refined by the predeclared stage/branch/provenance partition. Conditioning on one eliminated-variable branch bounds a factor's support by at most the number of table entries consistent with that branch. Cross-bucket duplicates are deliberately overcounted. Different signatures are **not** assumed to imply distinct canonical nodes.

For every admitted refinement:

`|K_reachable| <= U_Bk <= U_B0`.

## Anti-materialization lock

C90 execution may retain only state scaling with source factors, scopes, variables, elimination stages, operators and frozen finite signature classes. The implementation hard-fails if abstract-state records exceed `8192`.

It may not construct a C90 node dictionary/list/set; node tuples or fingerprints; per-intern or per-assignment traces; symbolic factor tables; a C90 compiled or serialized DAG; or selector-value state.

The exact symbolic compiler is callable only in the C72 control-certification phase.

## Controls

B1/B2/B3 are checked against exact C72 canonical node counts for every frozen algebra and both distinct frozen orders. A refinement is admitted to C90 only when its bound is at least the exact control count and no greater than B0. Controls falsify implementation mistakes; C90 soundness comes from the structural recurrence itself.

## Frozen physical-model readback

- `MemTotal = 13,605,830,656` bytes.
- `MemAvailable = 12,660,047,872` bytes.
- node-store engineering proxy = `338` bytes / retained canonical node.
- live-table slot proxy = `18` bytes / live entry.
- fixed process overhead = `256 MiB`.
- reserve = `2 GiB`.
- predicted peak must be at most `70%` of total RAM.

Derived certificate targets, not caps:

- S0/S1: `23,808,849` retained nodes.
- S2/S3: `23,808,740` retained nodes.

All gate decisions use integer arithmetic.

## Terminal outcomes

Only:

- `C90_CANONICAL_NODE_BOUND_CERTIFIES_PHYSICAL_ENVELOPE_CANDIDATE`;
- `C90_CANONICAL_NODE_BOUND_TIGHTENED__PHYSICAL_GATE_STILL_UNCERTIFIED`;
- `C90_CANONICAL_NODE_BOUND_NO_MATERIAL_TIGHTENING`;
- `C90_CANONICAL_NODE_BOUND_AUDIT_INDETERMINATE`;
- operational failure before scientific adjudication.

A positive physical-envelope candidate creates eligibility only for a separately governed exact-execution successor.

## Exclusions

No C90 materialization, frozen-307 validation, fifth S-method, new order search, coordinate/cap/physical-model amendment, empirical deduplication extrapolation, probabilistic distinct counting, semantic simplification, approximation, accelerator-native kernel, family/asymptotic/intractability claim, `QEC-CIRCUIT-003`, or `QLDPC-FORGE`.
