# QTR-QEC-CIRCUIT-002 — exact temporal TCM representation decomposition audit

## Status

`REFEREE_PROMOTED_BOUNDED`

Referee-promoted bounded representation authority is recorded only in the documentary overlay:

`reviews/QTR-QEC-CIRCUIT-REVIEW-002/promotion-record.json`

The immutable scientific registry and evidence remain exactly `candidate_executable_not_promoted`; documentary promotion does not rewrite those scientific snapshots.

Scientific execution was authorized only by Council contract #82 and Human Steward authorization comment `5323092968`:

`ADOPT_WITH_AMENDMENTS__AUTHORIZE_QEC_CIRCUIT_002_ONLY`

Documentary promotion was separately authorized on Issue #86 by Human Steward comment `5335168196`:

`ADOPT_WITH_AMENDMENTS__AUTHORIZE_QEC_CIRCUIT_002_DOCUMENTARY_PROMOTION_ONLY`

Execution docket: #83.
Scientific review: #85.
Documentary referral: #86.

Protected scientific merge: `e85d67619a0d739fe039cca8f271f9a32ae2f3db`.
Reviewed exact head: `695ea1da951cd2b4f9d5a6a07c30b090cfd37709`.

## Question

Can the already-certified C18 three-round phenomenological temporal decoding problem be represented by one of the predeclared exact auxiliary-state decompositions in a way that moves exact temporal TCM compilation inside the unchanged `QEC-CIRCUIT-001` resource envelope?

The governing principle is to resolve the representation problem before enlarging the physics problem.

## Pre-outcome freeze

Before any successor structural width was inspected, the complete representation family was committed in:

`registry/qec-circuit-002-manifest.json`

First manifest commit:

`ace579c9f3856349da612ac243d37970967f2562`

Manifest payload SHA-256:

`9ba84244f828bc0c4f9f128e54d2c89693930c2280540f9dc420ae13e964aa29`

No representation was added, removed, repaired, or retuned after width inspection.

## Frozen scientific object

This work package changes representation only. It transports without modification from `QEC-CIRCUIT-001`:

- protected C18 `[[18,4,4]]` code, X-error sector;
- three noisy phenomenological syndrome rounds plus perfect terminal readout;
- 75-bit temporal fault object and coordinate order;
- 28x75 detector map, rank 28, digest `960701757ef5c223d4ed96070508472e4f37feef92aec69d15b175bc078dbcb7`;
- complete 2,851-history elementary fault weight-0..2 corpus;
- exact detector-fiber ambiguity;
- independent elementary-fault probability `p=0.1`;
- exact sum-product objective;
- protected four-bit logical-selector semantics;
- terminal correctness oracle and deterministic correction representatives;
- all exact-TCM resource caps.

The protected conventional finite results are transported, not rerun:

- `TEMP_BP_OSD_CS_7`: `2520/2851`;
- `TEMP_BP_MIN_SUM`: `2430/2851`;
- `TEMP_BP_SUM_PRODUCT`: `1736/2851`.

## Representation ladder

The frozen family is:

1. `R0_BASELINE_107_FACTOR` — protected `QEC-CIRCUIT-001` representation;
2. `R1_TERMINAL_DIRECT_AUX` — explicit terminal-error auxiliaries `e[q]`;
3. `R2_TERMINAL_CHAIN_AUX` — temporal accumulator `u[q]` followed by terminal `e[q]`;
4. `R3_CAUSAL_STATE_CHAIN` — `R2` plus exact round-local syndrome-increment auxiliaries `r[t,c]` and local detector relations.

All four use deterministic min-fill with lowest canonical variable index as the primary ordering rule. No hand-designed order, auxiliary-order exception, portfolio, stochastic search, adaptive contraction, or post-outcome tuning is authorized.

## Exact semantic-equivalence receipts

All three successor representations satisfy the predeclared exact semantic gate.

### R1

- terminal direct XOR relation is uniquely extended for all 8 local original assignments;
- terminal relation receipt: `ee26e328e893db21dbd860887541edc44545515ca583e77d257fce3d82f175bc`;
- all four logical-selector rewrites are exhaustively checked on their local supports;
- selector receipt: `2a7c7aabf03c645ba3028d7989c551d82b7ebfb001c9f17cc902bc803dd8e2d9`.

### R2

- two-step terminal accumulator is uniquely extended for all 8 local original assignments;
- terminal chain receipt: `58a2480b08b25bb23ad3643ce7c9c799f8ac7c2990490ddfe98f022fb2feb4de`;
- terminal `e` equals the direct three-round XOR exactly;
- selector receipt is unchanged: `2a7c7aabf03c645ba3028d7989c551d82b7ebfb001c9f17cc902bc803dd8e2d9`.

### R3

- inherits the certified R2 terminal chain;
- certifies all 21 round/check syndrome-increment relations by complete local truth tables;
- syndrome-increment receipt: `a072cb066a3f7a780b491dc7c5bb771b5fe914506ef3cf5585bcd28504f3e1e8`;
- directly checks recovery of the replaced detector parity relations over 4,494 local boundary assignments after unique `r` substitution;
- detector rewrite receipt: `0ef88c6ac026714ad9132ce38759268c969f4557595c728e3d990b9e5962497d`;
- selector receipt is unchanged.

No auxiliary multiplicity or normalization factor is introduced. A semantic failure would invalidate all width claims for that candidate; none occurred.

## Structural result

The unchanged peak joint-table cap is `2^20 = 1,048,576` entries.

| Representation | Variables | Factors | Primal edges | Primary min-fill width | Peak arity | Predicted peak table | Status |
|---|---:|---:|---:|---:|---:|---:|---|
| `R0_BASELINE_107_FACTOR` | 75 | 107 | 782 | 34 | 35 | `2^35` | bound exhausted |
| `R1_TERMINAL_DIRECT_AUX` | 93 | 125 | 635 | 36 | 37 | `2^37` | bound exhausted |
| `R2_TERMINAL_CHAIN_AUX` | 111 | 143 | 635 | 36 | 37 | `2^37` | bound exhausted |
| `R3_CAUSAL_STATE_CHAIN` | 132 | 164 | 586 | 36 | 37 | `2^37` | bound exhausted |

Thus the three predeclared exact auxiliary-state rewrites do not improve the frozen named-order compilation boundary. Under deterministic min-fill they increase the induced width from 34 to 36.

All four structural audits completed. Every row stopped before inadmissible table materialization.

The bounded promoted scientific result is:

`TEMPORAL_PREDECLARED_DECOMPOSITION_FAMILY_EXHAUSTED`

This means only that this finite, predeclared representation family has been exhausted under the unchanged deterministic compiler policy and resource envelope.

It is not a global treewidth result, an intrinsic-intractability result, a runtime result, a memory result, or a theorem about temporal tensor decoding in general.

## Quality boundary

No successor representation compiled inside the frozen caps. Therefore:

- no exact temporal TCM selector-score matrix was generated;
- no new TCM corrections were generated;
- TCM quality remains undefined;
- no TCM-vs-conventional quality ordering is defined;
- the protected conventional totals remain transport-only context.

## Evidence identities

Full exact report payload SHA-256:

`90d73b06e3778fea7322435f1ff7db74fc3cb708038057ef338643082aa25c28`

Committed compact evidence:

`evidence/QEC-CIRCUIT-002-report.json`

Compact evidence payload SHA-256:

`cb9915e3d9bb32dc5abf1705c3dc7709082b79e3c5b91b391fb2aca0e632fcc3`

The scientific evidence and registry status remain exactly `candidate_executable_not_promoted`. The scientific merge and later documentary promotion are recorded outside those immutable scientific files.

## Review and replay provenance

The exact reviewed scientific head was `695ea1da951cd2b4f9d5a6a07c30b090cfd37709`; Referee scientific disposition comment `5334661008` approved only the bounded scientific merge. Protected merge readback fixed scientific merge `e85d67619a0d739fe039cca8f271f9a32ae2f3db`.

Authoritative exact-head machine evidence includes exact decomposition replay run `32096861740`, GCL conformance run `32096862004`, QTR validation run `32096861753`, and CodeQL receipt comment `5334640843`. Earlier QTR attempts under run `32095587023` remain diagnostic/non-authoritative timeout provenance. The final test-only repair did not change the manifest, evaluator, evidence, registry, representation family, or scientific result.

## Explicit exclusions

This result creates no authority or claim for:

- gate-level syndrome extraction;
- ancillas, hook errors, CNOT/gate propagation, or gate-location faults;
- changed physical noise models or full-Pauli decoding;
- error-rate or round-count sweeps;
- thresholds or pseudothresholds;
- runtime or memory superiority/inferiority;
- global treewidth;
- intrinsic intractability;
- family or asymptotic scaling;
- learned decoding;
- adaptive/stochastic contraction-order search;
- `QEC-CIRCUIT-003` or any later circuit subgate;
- `QLDPC-FORGE`;
- autonomous code, decoder, circuit, or architecture search.

`QEC-CIRCUIT-003` remains unauthorized. `QLDPC-FORGE` remains unauthorized.

## Documentary promotion boundary

The documentary overlay may promote only the bounded scientific result and its completed provenance chain. It does not convert the underlying scientific status strings into promotion authority.

Final documentary promotion, if merged after fresh exact-head gates and documentary review, is represented only by:

`reviews/QTR-QEC-CIRCUIT-REVIEW-002/promotion-record.json`

No downstream authority follows from that record.
