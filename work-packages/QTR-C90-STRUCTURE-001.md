# QTR-C90-STRUCTURE-001 — exact structural-work reduction

Status: `AUTHORIZED_EXECUTION__MANIFEST_FIRST__NO_PROMOTION_AUTHORITY`

## Authority

- protected start: `4182946ec5bdfda9de1443124624bd084a5acfda`;
- Council contract: Issue #97;
- Referee recommendation: Issue #97 comment `5383330531`;
- Human Steward authorization: Issue #97 comment `5383415686`, exact disposition `ADOPT_WITH_AMENDMENTS__AUTHORIZE_QTR_C90_STRUCTURE_001_ONLY`;
- execution docket: Issue #98.

This work package changes only the exact compiler/representation structure for the already-frozen finite C90 inference problem. It does not change the code, physical model, bases, objective, correctness oracle, selector corpus, scientific backend, or inherited resource caps.

## Predeclared family

The complete family is frozen before transformed C90 outcome inspection:

- `S0_BASELINE`: protected sequential deterministic-min-fill variable elimination;
- `S1_GF2_CONSTRAINT_ELIMINATION`: canonical exact GF(2) elimination only for hard affine/parity indicator constraints. The protected weighted local factors have full support under all three protected algebras, so an absence of eligible hard constraints is represented as an exact identity transformation, not as permission to reinterpret weighted factors as equations;
- `S2_SEPARATOR_INTERFACE_COMPILATION`: deterministic exact junction-tree/interface construction induced by the protected min-fill triangulation. Maximal bags, maximum-intersection spanning tree, root, factor assignment, separators, child order, and tie-breaking are all frozen by the manifest;
- `S3_GF2_PLUS_SEPARATOR`: exactly `S1` followed by `S2`, with no hybrid-specific choices.

No fifth method, post-outcome order portfolio, new separator heuristic, or adaptive search is permitted.

## Controls

C72 is the primary already-protected larger exact control. `S2`/`S3` must reproduce the protected 300-selector validation-output digest before their C90 row is adjudicative. C18 is auxiliary only if needed and does not create new interface authority.

`S1` is machine-checked against the protected algebra definitions. A weighted factor is never treated as a GF(2) equation.

## Accounting

Every method has a method-native ledger and a normalized cross-method ledger. Preprocessing is counted. The normalized vocabulary includes GF(2) row/pivot work, substitutions, graph work, separator construction, factor/interface assignment evaluations, table reads/writes, exact arithmetic/comparison, node interning, projection, bitset reconstruction, and validation.

A method cannot receive a resource pass from a lower bound alone. `unknown` is fail-closed.

Frozen caps per algebra:

- peak joint/interface entries: `104857600`;
- factor/constraint entry evaluations: `134217728`;
- retained nodes/entries: `4194304`;
- canonical serialized bytes: `536870912`;
- compilation AOP events: `2147483648`.

## Execution phases

1. **A — freeze:** manifest, evaluator, workflow, tests, job and hosted executor are committed before transformed C90 outcomes are inspected.
2. **B — control:** machine-check exact control semantics. A failed method is rejected before C90 adjudication.
3. **C — static C90:** non-materializing native and normalized ledgers for every control-certified method.
4. **D — conditional exact materialization:** only a method with certified passes on every deterministic cap may run, in fresh CPU-reference subprocesses on the admitted Colab runner. `MemAvailable` is reread before each method but cannot override a deterministic cap.
5. **E — frozen 307:** only after admissible exact materialization, on the same frozen C90 selector set.

## Evidence and promotion

All scientific result surfaces remain `candidate_executable_not_promoted` until exact-head machine replay and separate scientific review. Any later promotion must preserve the scientific registry/evidence snapshot and use a separate documentary authority overlay.

## Explicit exclusions

No cap increase; no additional hardware as a substitute for structural work; no GPU/TPU-native scientific kernel; no approximation, pruning, tensor truncation, lossy compression or Monte Carlo; no C108/C144/C288/C784 execution; no global-treewidth, intrinsic-intractability, asymptotic, runtime, memory-performance or hardware claim; no gate-level `QEC-CIRCUIT-003`; no autonomous search; no `QLDPC-FORGE` authority.
